pipeline {
  agent { label 'dev' } // match the label you gave to the agent
  stages {
    stage('Test') {
      steps {
        sh 'echo Hello from Jenkins Agent!'
        sh 'hostname'
      }
    }
  }
}